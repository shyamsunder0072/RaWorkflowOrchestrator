# expose models(after loading them) and associated class-maps with them
# from airflow.operators.tfserving.audio.speech_to_text.models.version1 import conf as params
# from airflow.operators.tfserving.audio.speech_to_text.models.version1.speech_to_text_model import SpeechToTextModel

# _model_obj = SpeechToTextModel()
# _ = _model_obj.load_speech_to_text_model()
# speech_to_text_model, lm, trie = _model_obj.run()
